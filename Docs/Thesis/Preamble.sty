\ProvidesPackage{Preamble}

%Packages:
    %Test package:
    \usepackage{blindtext}
    %Graphics packages:
    \usepackage{graphicx} % Required for inserting images
    \renewcommand{\figurename}{Rysunek} % Change Figure to Rysunek in \caption
    % Font:
    \usepackage{fontspec}
    \setmainfont{Arial} %Use arial according to university requirements
    %PDF packages:
    \usepackage{pdfpages}
    %Packages for multifile project management:
    \usepackage{subfiles}
    \usepackage[a4paper, inner=3.5cm, outer=2.5cm, top=2.5cm, bottom=2.5cm]{geometry} %Not used yet
    %Sections and chapters customization:
    \usepackage{titlesec} %Not used yet
    %Text aligment customization:
    \usepackage{ragged2e}
    \justifying % Spread text on the entire page
    %References management:
    \usepackage{hyperref} %Is needed for biblatex package.
    %Bibliography management:
    \usepackage[backend=biber,
                hyperref=true, 
                isbn=true
                ]{biblatex}
    \addbibresource{Bibliography.bib}
    %Glossary management:
    \usepackage[acronym]{glossaries}
    \makeglossaries
    \usepackage{Sections/ListOfSymbolsAndAbbreviations}
    \usepackage[skip=1.5pt plus 1pt, indent=15pt]{parskip}
    \usepackage{indentfirst} % indent first paragraph in section.
    \usepackage{float} % For using \begin{figure}[H] to place images at the exact locations.
    \usepackage{subcaption} % For Subfigures
    \usepackage{cleveref} % For cref
    % Redefine cleveref names for figures:
    \crefname{figure}{rys.}{rys.}
    \Crefname{figure}{Rys.}{Rys.}
    \usepackage{xcolor}
    \usepackage{listings}
    % Configuration for code listings:
    \lstdefinestyle{mystyle}{
        backgroundcolor=\color{gray!10},
        showspaces=false,
        showstringspaces=false,
        basicstyle=\footnotesize,
%        numbers=left,
%        numberstyle=\small,
%        stepnumber=1,
%        numbersep=10pt,
        frame=lines,
        keywordstyle=\bfseries
    }
    \lstset{style=mystyle}
    \renewcommand{\lstlistingname}{Przykład}
    % For pseudocode blocks:
    \usepackage{algorithm}
    \usepackage{algpseudocode}
    \usepackage{float}
    \floatname{algorithm}{Algorytm}
    \usepackage{caption}
    \captionsetup[algorithm]{font=small, labelfont=bf}
    \crefname{algorithm}{Alg.}{Alg.}
    \Crefname{algorithm}{Alg.}{Alg.}
    % Math packages:
    \usepackage{amsmath}
    \usepackage{unicode-math}
    \usepackage{setspace}
    \renewcommand{\thepage}{\fontsize{9pt}{9pt}\selectfont\arabic{page}}
    \usepackage[polish]{babel}
    % Centerign captions:
    \usepackage[justification=centering]{caption}

%Other definitions:
    %Change the title of the Table of content:
    \renewcommand*\contentsname{Spis treści}
    %Change the title of the Acronyms list:
    \renewcommand{\acronymname}{Skróty i oznaczenia}
    % Change the title of the List of Figures:
    \renewcommand{\listfigurename}{Spis rysunków}
    %Page numbering style:
    \pagenumbering{arabic}
    % Distance between lines:
    \linespread{1.5}
    \sloppy
    \raggedbottom
    \usepackage{placeins}